import {URLBase} from "./URLBase";

export class URLArea extends URLBase {
   static readonly areas = URLArea.baseUrl + "/area/areas"
   static readonly reportes = URLArea.baseUrl + "/area/reportes"
}